{
    "name": "cwsp-shared",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=24"
    },
    "description": "Shared library extras for CWSP apps (AI helpers, document tools, OPFS, transport). Not a SPA/CRX app — Chrome extension builds live in apps/CWSP-crx.",
    "exports": {
        ".": "./src/index.ts",
        "./shared": "./src/shared/index.ts",
        "./shared/*": "./src/shared/*",
        "./types": "./src/types/build-flags.d.ts",
        "./types/*": "./src/types/*"
    },
    "scripts": {
        "build": "node -e \"console.log('[cwsp-shared] library package — no Vite SPA/CRX build; import via package exports.')\"",
        "test:test": "echo 'No tests configured for cwsp-shared' && exit 0"
    }
}
